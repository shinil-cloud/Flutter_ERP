import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class Constants {
  late BuildContext context;
  Constants(this.context);

  double get scrnWidth => MediaQuery.of(context).size.width;
  double get scrnHeight => MediaQuery.of(context).size.height;

  // Color primaryColor = Color(0xff4b0082);
  // Color primaryColorDark = Color(0xff1a002e);
  // Color primaryColor50 = Color(0x504b0082);
  // Color primaryColor90 = Color(0xCC4b0082);

  // Color primaryColorLight = Color(0xffb395c9);
  // Color textColor = Color(0xffffffff);
  // String appName = "Nilaav";

  //000E7A
}
